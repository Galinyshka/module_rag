"""
Все промпты RAG-пайплайна в одном месте.
"""

# ---------------------------------------------------------------------------
# Router
# ---------------------------------------------------------------------------

ROUTER_PROMPT = """\
Ты — классификатор запросов к корпусу рабочих программ учебных дисциплин вуза.

Типы запросов:
  single.simple  — точечный фактологический вопрос об ОДНОЙ дисциплине
                   (часы, компетенции, темы, форма контроля, литература и т.п.)
  single.global  — развёрнутый или обобщающий вопрос об ОДНОЙ дисциплине
  multi.relation — сравнение или выявление связей между КОНКРЕТНЫМИ дисциплинами
                   (названия дисциплин явно указаны в запросе)
  multi.global   — запрос по всему корпусу или множеству дисциплин без явного перечисления

Задача:
1. Определи тип запроса.
2. Извлеки названия дисциплин, упомянутых в запросе (на русском языке; пустой список если не упомянуты).
3. Определи, является ли запрос «временно-числовым»: вопрос о часах, зачётных единицах,
   семестрах, распределении нагрузки, форме итоговой аттестации.

Отвечай ТОЛЬКО валидным JSON без markdown-блоков:
{{
  "query_type": "single.simple|single.global|multi.relation|multi.global",
  "disciplines": ["название1", "название2"],
  "is_time_query": true|false,
  "reasoning": "краткое пояснение"
}}

Запрос пользователя:
{query}
"""

# ---------------------------------------------------------------------------
# Query Expander
# ---------------------------------------------------------------------------

PARAPHRASE_PROMPT = """\
Сгенерируй 3 альтернативные формулировки запроса на русском языке.
Используй разную лексику и синтаксис, сохраняя смысл.
Отвечай ТОЛЬКО валидным JSON:
{{"paraphrases": ["вариант1", "вариант2", "вариант3"]}}

Запрос: {query}
"""

DECOMPOSE_PROMPT = """\
Разбей составной запрос на атомарные подзапросы.
Каждый подзапрос должен быть самодостаточным и затрагивать ровно один аспект одной дисциплины.
Отвечай ТОЛЬКО валидным JSON:
{{"sub_queries": ["подзапрос1", "подзапрос2", ...]}}

Запрос: {query}
"""

# ---------------------------------------------------------------------------
# Generation — 4 варианта по типу запроса
# ---------------------------------------------------------------------------

GENERATE_SINGLE_SIMPLE = """\
Ты — ассистент, отвечающий на вопросы по рабочей программе дисциплины.
Дай точный и краткий ответ, опираясь ТОЛЬКО на предоставленный контекст.
Если информации нет — прямо скажи об этом.

Вопрос: {query}

Контекст:
{context}

Ответ:"""

GENERATE_SINGLE_GLOBAL = """\
Ты — ассистент, отвечающий на вопросы по рабочей программе дисциплины.
Синтезируй развёрнутый ответ на основе предоставленного контекста.
Структурируй ответ, если это помогает восприятию.
Если информации нет — прямо скажи об этом.

Вопрос: {query}

Контекст:
{context}

Ответ:"""

GENERATE_MULTI_RELATION = """\
Ты — ассистент, сравнивающий рабочие программы дисциплин.
Выполни сравнительный анализ на основе предоставленного контекста.
Используй структурированный формат (таблицу или пункты) для наглядности.
Опирайся ТОЛЬКО на предоставленные данные.

Вопрос: {query}

Контекст:
{context}

Ответ:"""

GENERATE_MULTI_GLOBAL = """\
Ты — ассистент, анализирующий корпус рабочих программ дисциплин.
Синтезируй обобщающий ответ по всем релевантным дисциплинам из контекста.
Перечисли конкретные дисциплины, если они подходят под условие вопроса.
Опирайся ТОЛЬКО на предоставленные данные.

Вопрос: {query}

Контекст:
{context}

Ответ:"""

# Time Filter — специализированный промпт для числовых/временных запросов
GENERATE_TIME_QUERY = """\
Ты — ассистент, извлекающий числовые и структурные данные из рабочей программы.
Ответь ТОЛЬКО конкретным значением (число, тип аттестации, форма контроля).
Без вступлений, пояснений и контекста — только факт.

Вопрос: {query}

Контекст:
{context}

Ответ (только факт):"""

GENERATE_PROMPTS = {
    "single.simple":  GENERATE_SINGLE_SIMPLE,
    "single.global":  GENERATE_SINGLE_GLOBAL,
    "multi.relation": GENERATE_MULTI_RELATION,
    "multi.global":   GENERATE_MULTI_GLOBAL,
}

# ---------------------------------------------------------------------------
# Verification
# ---------------------------------------------------------------------------

VERIFY_PROMPT = """\
Оцени качество ответа на вопрос пользователя.

Критерии проверки:
1. Ответ содержит конкретную запрашиваемую информацию (не уклончивый отказ без причины).
2. Вся информация в ответе подкреплена предоставленным контекстом (нет «галлюцинаций»).
3. Ответ соответствует типу вопроса (факт → конкретная цифра/факт; обзор → синтез).

Отвечай ТОЛЬКО валидным JSON:
{{
  "is_valid": true|false,
  "retry": true|false,
  "note": "краткое пояснение (1–2 предложения)"
}}

retry=true только если проблема в недостаточном контексте (стоит повторить поиск),
retry=false если проблема в формулировке ответа или данных нет в корпусе.

Вопрос: {query}

Контекст (использованный при генерации):
{context}

Ответ модели:
{answer}
"""
