QUESTIONS_PROMPT = """\
Ты помогаешь настроить поиск по учебным программам дисциплин.

Дан блок РПД. Сгенерируй ровно {n} вопросов, на которые этот блок содержит ответ.

Требования:
- Вопросы должны звучать как реальные запросы студента или преподавателя.
- Используй разные формулировки: сколько, какие, есть ли, как, что, когда.
- Вопросы должны быть конкретными и разными.
- Только вопросы, без нумерации, каждый с новой строки.

Важно:
- Не используй безличные ссылки: "эта тема", "этот раздел", "этот блок", "здесь", "данная тема".
- Если вопрос относится к теме/разделу/модулю — используй название блока.
- Вместо "что изучают по этой теме?" пиши: "что изучают в теме «{block_name}»?"
- Если название длинное — можно использовать ключевые слова без потери смысла.

Примеры:
Блок: "Тема 3: Словари и множества"
Что изучают в теме «Словари и множества»?
Сколько часов отведено на тему «Словари и множества»?

Блок: "Общие сведения о дисциплине"
Какая форма итоговой аттестации?
Сколько часов лекций в дисциплине?

Тип блока: {block_type}
Название блока: {block_name}

Текст блока:
{text}

Верни ровно {n} вопросов.
""".strip()


GENERATE_SINGLE_GLOBAL = """\
Ты — ассистент, отвечающий на вопросы по рабочей программе учебной дисциплины.

Правила:
- Опирайся ТОЛЬКО на предоставленный контекст
- Структурируй ответ: используй заголовки или списки если тем несколько
- Указывай конкретные значения (часы, коды компетенций, названия тем) — не пересказывай абстрактно
- Если какой-то аспект вопроса не покрыт контекстом — явно укажи это
- Не придумывай детали которых нет в контексте

Вопрос: {query}

Контекст:
{context}

Ответ:"""

ROUTER_PROMPT = """\
Ты — классификатор запросов к корпусу рабочих программ учебных дисциплин (РПД) вуза.
РПД содержат: трудоёмкость (часы, ЗЕ), тематический план, компетенции, литературу,
формы аттестации, перечни вопросов для семинаров и самостоятельной работы.

ТИПЫ ЗАПРОСОВ:

single.simple — точечный фактологический вопрос об ОДНОЙ дисциплине.
  Признак: ищется конкретное значение — число, список, название.
  Примеры:
    «Сколько часов лекций по дисциплине X?»
    «Какие компетенции формирует курс линейной алгебры?»
    «Какая форма итогового контроля в дисциплине X?»

single.global — развёрнутый, обзорный или аналитический вопрос об ОДНОЙ дисциплине.
  Признак: требует синтеза нескольких разделов РПД, а не одного значения.
  Примеры:
    «Расскажи о дисциплине X подробно»
    «Как устроена самостоятельная работа в курсе X?»
    «Чему научит курс X?»

multi.relation — сравнение или выявление связей между дисциплинами,
  названия которых ЯВНО указаны в запросе.
  Примеры:
    «Чем различаются дисциплины X и Y?»
    «Какие темы пересекаются между X и Y?»

multi.global — запрос по множеству или всему корпусу дисциплин без явного перечисления.
  Признак: обобщающие слова «все», «какие», «в каких», «есть ли».
  Примеры:
    «В каких дисциплинах изучается Python?»
    «Какие курсы заканчиваются экзаменом?»
    «Где встречается тема машинного обучения?»

ПРАВИЛА КЛАССИФИКАЦИИ:
- Если в запросе одна дисциплина + конкретный факт → single.simple
- Если в запросе одна дисциплина + нет конкретного факта → single.global
- Если упомянуты ≥2 конкретных дисциплины → multi.relation
- Если дисциплина не названа или вопрос «по всем» → multi.global
- Поле disciplines: извлекай ТОЛЬКО явно упомянутые названия дисциплин на русском языке.
  Не придумывай названия. Если дисциплина не упомянута — [].

Отвечай ТОЛЬКО валидным JSON без markdown-блоков:
{{
  "query_type": "single.simple|single.global|multi.relation|multi.global",
  "disciplines": ["точное название из запроса"],
  "reasoning": "1 предложение — почему выбран этот тип"
}}

Запрос: {query}
"""

EXTRACT_PROMPT = """\
Ты извлекаешь названия дисциплин из запроса пользователя.

Допустимые дисциплины (использовать только их):
{rpd_names}

Правила:
- Все названия уже нормализованы и в lowercase.
- Верни ТОЛЬКО дисциплины из списка допустимых.
- Ищи точные совпадения по словам/фразам.
- Не придумывай новые дисциплины.
- Если найдено несколько дисциплин — верни ВСЕ.
- Если ничего не найдено — верни [].

Отвечай только JSON:

{{
  "disciplines": []
}}

Запрос: {query}
"""

CLASSIFY_PROMPT = """\
Ты классифицируешь запрос к корпусу РПД.

Запрос пользователя:
{query}

Найденные дисциплины:
{disciplines}

Типы:

single.simple
- одна дисциплина
- запрос конкретного факта:
  часы, ЗЕ, темы, литература, компетенции,
  форма контроля, экзамен/зачёт, список и т.п.

single.global
- одна дисциплина
- обзор / описание / анализ / чему учит /
  как устроен курс / расскажи подробно

multi.relation
- две или более дисциплины
- сравнение / отличие / связь / пересечения

multi.global
- дисциплины не указаны
- поиск по всему корпусу:
  какие курсы, где изучается, в каких дисциплинах,
  есть ли дисциплины с ..., все дисциплины и т.п.

not_found
- пользователь явно спрашивает про конкретную дисциплину,
  но такой дисциплины нет в списке

Правила:

1. Если найдено 2+ дисциплины -> multi.relation

2. Если найдена 1 дисциплина:
   - если вопрос про конкретный факт -> single.simple
   - иначе -> single.global

3. Если найдено 0 дисциплин:
   - если запрос похож на поиск по корпусу -> multi.global
   - если пользователь спрашивает конкретную дисциплину -> not_found

Отвечай только JSON:

{{
  "query_type": "single.simple|single.global|multi.relation|multi.global|not_found",
  "disciplines": []
}}
"""


DECOMPOSE_PROMPT = """\
Запрос затрагивает несколько дисциплин или несколько аспектов.
Разбей его на атомарные подзапросы — каждый про ОДИН аспект ОДНОЙ дисциплины.

Требования:
- Каждый подзапрос самодостаточен (можно задать отдельно)
- Сохраняй все упомянутые дисциплины и аспекты
- Не добавляй аспекты которых нет в оригинале

Пример:
  Вопрос: «Сравни объём лекций и форму контроля в Python и Java»
  Результат: [
    «Сколько часов лекций в дисциплине Python?»,
    «Форма итогового контроля в дисциплине Python?»,
    «Сколько часов лекций в дисциплине Java?»,
    «Форма итогового контроля в дисциплине Java?»
  ]

Отвечай ТОЛЬКО валидным JSON:
{{"sub_queries": ["подзапрос1", "подзапрос2", ...]}}

Запрос: {query}
"""

GENERATE_MULTI_RELATION = """\
Ты — ассистент, выполняющий сравнительный анализ рабочих программ дисциплин.

Правила:
- Опирайся ТОЛЬКО на предоставленный контекст
- Используй таблицу если сравниваются числовые или категориальные параметры
- Если таблица неудобна — структурируй по дисциплинам: «Дисциплина A: ... Дисциплина B: ...»
- Указывай конкретные значения, не ограничивайся «больше/меньше»
- Если по одной из дисциплин данных не хватает — явно отметь это

Вопрос: {query}

Контекст:
{context}

Ответ:"""

COMPARE_PROMPT = """\
Пользователь хочет сравнить несколько учебных дисциплин.

Вопрос: {query}

Ниже представлены материалы рабочих программ дисциплин (РПД):
{context}

Задача: напиши структурированное сравнение дисциплин по существу вопроса.
— Используй только данные из РПД выше, не придумывай.
— Если по какому-то аспекту данных нет — прямо укажи на это.
— Структурируй по критериям сравнения (не по дисциплинам).
— Отвечай на русском языке.
"""

COMPARE_PROMPT = """
Сравни учебные дисциплины на основе их рабочих программ (РПД).

Вопрос пользователя: {query}

РПД дисциплин:
{context}

Составь структурированное сравнение по следующим параметрам (если данные есть в РПД):

1. Семестр и трудоёмкость (ЗЕ, академические часы)
2. Форма итогового контроля (экзамен / зачёт)
3. Виды учебной работы (лекции, практики, лабораторные — сколько часов)
4. Формируемые компетенции (коды и формулировки)
5. Основные темы и разделы
6. Цели и задачи дисциплины

Правила:
— Для каждого параметра приводи конкретные значения из каждой дисциплины рядом.
— Если параметр есть только в одной дисциплине — укажи это явно.
— Если данных нет в РПД — напиши "нет данных", не придумывай.
— Не пиши общих слов о важности дисциплин.
— Отвечай на русском языке.
""".strip()

# prompts.py
PROMPT_CLASSIFY_MULTI = """
Пользователь задал вопрос, в котором упоминаются несколько учебных дисциплин.

Определи тип запроса:

multi.compare — пользователь хочет сравнить дисциплины в целом, без указания конкретного аспекта:
  "сравни дисциплину А и Б"
  "чем отличается А от Б"
  "что общего между А и Б"
  "в чём разница между А и Б"

multi.relation — пользователь спрашивает про конкретный аспект одновременно в нескольких дисциплинах:
  "сравни компетенции в дисциплине А и Б"
  "какие темы пересекаются в А и Б"
  "сколько часов лекций в А и Б"
  "есть ли лабораторные в А и Б"

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown:
{{"query_type": "multi.compare"}} или {{"query_type": "multi.relation"}}
""".strip()

SYNTHESIS_PROMPT = """\
Пользователь задал вопрос, который потребовал анализа нескольких дисциплин.
Ниже приведены частичные ответы по каждому аспекту вопроса.

Исходный вопрос: {query}

Частичные ответы:
{sub_answers}

Задача: на основе частичных ответов сформируй единый, связный, структурированный ответ.
— Не повторяй одно и то же дважды.
— Если ответы противоречат друг другу — укажи на это явно.
— Если по какому-то аспекту данных не хватило — кратко упомяни об этом.
— Отвечай на русском языке.
"""


SYNTHESIS_PROMPT = """
Пользователь задал вопрос: {query}

Ниже — ответы по каждому аспекту, полученные из рабочих программ дисциплин:
{sub_answers}

Задача: объедини частичные ответы в один связный ответ на вопрос пользователя.

Строгие правила:
— Используй ТОЛЬКО информацию из частичных ответов выше. Не добавляй ничего от себя.
— Если в частичном ответе сказано "данных нет" или "информация отсутствует" — так и передай, не заполняй пробел.
— Не делай выводов и оценок, которых нет в частичных ответах.
— Не анализируй дисциплины в целом — отвечай строго на заданный вопрос.
— Если данных недостаточно для ответа — честно укажи это.
— Отвечай на русском языке.
""".strip()


PROMPT_EXTRACT_DISCIPLINES = """
Ты — ассистент, который определяет, какие учебные дисциплины имеет в виду пользователь.

Тебе дан запрос пользователя и список дисциплин-кандидатов (отобранных заранее по схожести).

Твои возможные решения:

1. Если можешь уверенно определить дисциплину(ы) — верни:
{{"status": "found", "disciplines": ["точное название из кандидатов", ...]}}

2. Если кандидаты слишком похожи и непонятно, какую дисциплину имеет в виду пользователь — верни:
{{"status": "clarify", "candidates": ["кандидат 1", "кандидат 2"]"}}

3. Если ни один кандидат не подходит — верни:
{{"status": "not_found"}}

Правила:
- Используй ТОЛЬКО названия из списка кандидатов, без изменений.
- В "found" можно вернуть несколько дисциплин, если пользователь явно упомянул несколько.
- В "clarify" перечисляй только реально неоднозначные кандидаты (не все подряд).
- Если один из кандидатов явно точнее совпадает с запросом — выбирай его (status: found), не проси уточнения.

Кандидаты:
{candidates}

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown.
""".strip()

PROMPT_EXTRACT_QUERY_DISCIPLINE = """
Из запроса пользователя извлеки фрагмент, который похож на название учебной дисциплины.

Правила:
- Верни только предполагаемое название — без лишних слов ("по дисциплине", "курс", "предмет", номера семестров, вопросов и т.д.)
- Если в запросе несколько возможных названий — верни все.
- Если ничего похожего на название дисциплины нет — верни пустой список.
- Не исправляй и не дополняй — верни ровно то, что написал пользователь.

Примеры:
  "Сколько часов лекций по линейной алгебре?"
  → {{"names": ["линейная алгебра"]}}

  "Примерные вопросы к контрольной по дисциплине Алгоритмы и структура данных в Python для 1 семестра"
  → {{"names": ["Алгоритмы и структура данных в Python"]}}

  "Сравни машинное обучение и глубокое обучение"
  → {{"names": ["машинное обучение", "глубокое обучение"]}}

  "Какая погода сегодня?"
  → {{"names": []}}

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown.
""".strip()

PROMPT_CLASSIFY_GLOBAL_SUBTYPE = """
Пользователь задаёт вопрос обо всём корпусе учебных дисциплин — без указания конкретной дисциплины.

Твоя задача:
1. Определить подтип запроса.
2. Извлечь ключевой термин, который нужно искать (если применимо).

---

Подтипы и критерии выбора:

"catalog"
  Пользователь хочет получить список дисциплин или общую информацию о корпусе.
  Примеры: «Какие дисциплины есть?», «Перечисли все курсы», «Что у нас преподаётся?»
  entity: "" (термин не нужен)

"competency_exact"
  В запросе есть конкретный код компетенции (буквы + цифры, например ОПК-2, DL-1, ПК-3).
  Примеры: «В каких дисциплинах есть компетенция ОПК-2?», «Где встречается DL-1?»
  entity: код компетенции в точном виде из запроса (например "ОПК-2")

"competency_semantic"
  Пользователь описывает тему или навык компетенции, но без кода.
  Примеры: «Где есть компетенции про нейронные сети?», «В каких курсах формируется навык работы с данными?»
  entity: ключевая фраза из запроса (например "нейронные сети")

"topic_search"
  Пользователь ищет, в каких дисциплинах изучается конкретная тема или концепция.
  Примеры: «Где изучается SVD?», «В каких курсах есть тема про рекомендательные системы?», «Где проходят коллаборативную фильтрацию?»
  entity: название темы или концепции (например "SVD" или "коллаборативная фильтрация")

"global_semantic"
  Запрос семантически ищет по всему корпусу, но не попадает в категории выше.
  Примеры: «Где упоминается Docker?», «В каких дисциплинах используется MovieLens?», «Где есть задания на Python?»
  entity: ключевое слово или фраза для поиска (например "Docker")

---

Запрос пользователя: {query}

Ответь строго JSON без пояснений:
{{
  "subtype": "<catalog|competency_exact|competency_semantic|topic_search|global_semantic>",
  "entity": "<извлечённый термин или пустая строка>"
}}
""".strip()