"""Все промпты RAG-пайплайна.

PROMPT_EXTRACT_QUERY_DISCIPLINE - LLM извлекает из запроса фрагмент, похожий на название дисциплины. Возвращает список названий (может быть пустым).
PROMPT_EXTRACT_DISCIPLINES - LLM определяет, какие учебные дисциплины имеет в виду пользователь из списка fuzzy-кандидатов. Возвращает статус (found/clarify/not_found) и список дисциплин или кандидатов для уточнения.
PROMPT_CLASSIFY_SINGLE - LLM классифицирует запрос с одной дисциплиной. Возвращает query_type: single.simple или single.global.
PROMPT_CLASSIFY_ZERO - LLM классифицирует запрос без дисциплин. Возвращает query_type: multi.global, not_found или irrelevant.
PARAPHRASE_PROMPT 
DECOMPOSE_PROMPT 
HYDE_PROMPT
GENERATE_SINGLE_SIMPLE
GENERATE_SINGLE_GLOBAL
GENERATE_MULTI_RELATION
GENERATE_MULTI_GLOBAL
FACT_EXTRACT_PROMPT
VERIFY_PROMPT 
"""

# ---------------------------------------------------------------------------
# Router
# ---------------------------------------------------------------------------

PROMPT_EXTRACT_QUERY_DISCIPLINE = """
Из запроса пользователя извлеки фрагмент, который похож на название учебной дисциплины.

Правила:
- Верни только предполагаемое название — без лишних слов ("по дисциплине", "курс", "предмет", номера семестров, вопросов и т.д.)
- Если в запросе несколько возможных названий — верни все.
- Если ничего похожего на название дисциплины нет — верни пустой список.
- Не исправляй и не дополняй — верни ровно то, что написал пользователь.

Примеры:
  "Сколько часов лекций по линейной алгебре?"
  → {{"names": ["линейная алгебра"]}}

  "Примерные вопросы к контрольной по дисциплине Алгоритмы и структура данных в Python для 1 семестра"
  → {{"names": ["Алгоритмы и структура данных в Python"]}}

  "Сравни машинное обучение и глубокое обучение"
  → {{"names": ["машинное обучение", "глубокое обучение"]}}

  "Какая погода сегодня?"
  → {{"names": []}}

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown.
""".strip()

PROMPT_EXTRACT_DISCIPLINES = """
Ты — ассистент, который определяет, какие учебные дисциплины имеет в виду пользователь.

Тебе дан запрос пользователя и список дисциплин-кандидатов (отобранных заранее по схожести).

Твои возможные решения:

1. Если можешь уверенно определить дисциплину(ы) — верни:
{{"status": "found", "disciplines": ["точное название из кандидатов", ...]}}

2. Если кандидаты слишком похожи и непонятно, какую дисциплину имеет в виду пользователь — верни:
{{"status": "clarify", "candidates": ["кандидат 1", "кандидат 2"]"}}

3. Если ни один кандидат не подходит — верни:
{{"status": "not_found"}}

Правила:
- Используй ТОЛЬКО названия из списка кандидатов, без изменений.
- В "found" можно вернуть несколько дисциплин, если пользователь явно упомянул несколько.
- В "clarify" перечисляй только реально неоднозначные кандидаты (не все подряд).
- Если один из кандидатов явно точнее совпадает с запросом — выбирай его (status: found), не проси уточнения.

Кандидаты:
{candidates}

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown.
""".strip()

PROMPT_CLASSIFY_SINGLE = """
Ты — ассистент, который классифицирует вопросы об учебных дисциплинах (РПД).

Определи тип запроса:

single.simple — вопрос об одном конкретном факте или поле РПД:
  - сколько часов / зачётных единиц
  - какая форма контроля (экзамен, зачёт)
  - в каком семестре
  - кто ведёт
  - какие компетенции формирует
  - есть ли лабораторные / практики

single.global — запрос на большой раздел, список или обзор дисциплины целиком:
  - примерные вопросы к экзамену / контрольной / зачёту
  - перечень тем / список тем раздела
  - список литературы / источников
  - критерии оценки / балльная система
  - задания для самостоятельной работы
  - чему учит курс / расскажи подробно / опиши программу
  - как устроен курс / структура дисциплины
  - содержание семестра

Правило: если ответ на вопрос — это большой текст или список (а не одно-два слова / число) → single.global.

Примеры:
  "Сколько часов лекций?" → single.simple
  "Какая форма итогового контроля?" → single.simple
  "В каком семестре изучается?" → single.simple
  "Какие компетенции формирует дисциплина?" → single.simple
  "Примерные вопросы к контрольной работе" → single.global
  "Вопросы для подготовки к экзамену" → single.global
  "Какие темы изучаются в 1 семестре?" → single.global
  "Расскажи о содержании курса" → single.global
  "Какая литература рекомендована?" → single.global
  "Как оценивается работа студентов?" → single.global
  "Какие задания для самостоятельной работы?" → single.global

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown:
{{"query_type": "single.simple"}} или {{"query_type": "single.global"}}
""".strip()

PROMPT_CLASSIFY_ZERO = """
Ты — ассистент, который классифицирует учебные запросы.

Пользователь задал запрос, в котором не удалось распознать ни одной
дисциплины из списка учебных программ (РПД).
Определи тип запроса:

- "multi.global"  — запрос касается всего корпуса дисциплин:
  какие дисциплины есть, где изучается тема X, в каких курсах упоминается Y.

- "not_found"     — пользователь спрашивает про конкретную дисциплину,
  которой нет в системе (неизвестный курс, вымышленное название).

Список возможных дисциплин:
{disciplines}


- "irrelevant"    — запрос вообще не относится к учебным программам.

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown:
{{"query_type": "multi.global"}} или {{"query_type": "not_found"}} или {{"query_type": "irrelevant"}}
""".strip()


PROMPT_CLASSIFY_MULTI = """
Пользователь задал вопрос, в котором упоминаются несколько учебных дисциплин.

Определи тип запроса:

multi.compare — пользователь хочет сравнить дисциплины В ЦЕЛОМ, без указания конкретного аспекта:
  "сравни дисциплину А и Б"
  "чем отличается А от Б"
  "что общего между А и Б"
  "расскажи про А и Б"

multi.relation — пользователь спрашивает про КОНКРЕТНЫЙ АСПЕКТ одновременно в нескольких дисциплинах.
  Признак: в вопросе есть предметное дополнение — что именно сравнить/найти/показать.
  "сравни литературу по А и Б"           ← конкретный аспект: литература
  "сравни компетенции в А и Б"           ← конкретный аспект: компетенции
  "сколько часов лекций в А и Б"         ← конкретный аспект: часы лекций
  "какие темы пересекаются в А и Б"      ← конкретный аспект: темы
  "есть ли лабораторные в А и Б"         ← конкретный аспект: лабораторные
  "в каком семестре А и Б"               ← конкретный аспект: семестр

Ключевое правило: если в вопросе можно выделить ЧТО именно нужно найти или сравнить — это multi.relation.
Если вопрос про дисциплины в целом — это multi.compare.

Запрос пользователя:
{query}

Ответь строго в формате JSON, без пояснений и markdown:
{{"query_type": "multi.compare"}} или {{"query_type": "multi.relation"}}
""".strip()
# ---------------------------------------------------------------------------
# Query Expander
# ---------------------------------------------------------------------------

PARAPHRASE_PROMPT = """\
Ты помогаешь улучшить поиск по корпусу рабочих программ учебных дисциплин (РПД) вуза.
РПД написаны в академическом стиле и содержат: трудоёмкость, компетенции,
тематические планы, перечни вопросов, формы аттестации.

Сгенерируй {num_paraphrases} альтернативные формулировки запроса ниже.
Требования:
- Используй терминологию РПД: «трудоёмкость», «аудиторная нагрузка», «компетенция», «индикатор»
- Варьируй синтаксис: вопрос / утверждение / именная группа
- Сохраняй точный смысл, не добавляй новых условий

Верни **только** один JSON объект в следующем формате. Ничего больше.
{{"paraphrases": ["вариант1", "вариант2", "вариант3"]}}

Запрос: {query}
"""

DECOMPOSE_EXPAND_PROMPT = """
Ты помощник для системы поиска по учебным программам.

Исходный запрос сравнивает несколько дисциплин: {disciplines}

Запрос пользователя:
"{query}"

Задача:
1. Разбей запрос на подзапросы — по одному аспекту на каждую дисциплину.
2. Для каждого подзапроса сгенерируй {num_paraphrases} перефразировки.
3. Укажи, к какой дисциплине относится каждый подзапрос (точное название из списка выше).

Верни ТОЛЬКО JSON без пояснений:
{{
  "sub_queries": [
    {{
      "discipline": "<точное название дисциплины>",
      "original": "<подзапрос>",
      "paraphrases": ["<вариант 1>", "<вариант 2>", "<вариант 3>"]
    }}
  ]
}}
"""

HYDE_PROMPT = """\
Ты — фрагмент рабочей программы учебной дисциплины вуза.
Напиши короткий текст (3–5 предложений) который мог бы содержать ответ на вопрос ниже.

Строго следуй формату РПД:
- Используй академический стиль и характерную терминологию
- Включай конкретные числа (часы, ЗЕ) если вопрос про трудоёмкость
- Упоминай коды компетенций (вида ПКН-1, УК-2) если вопрос про компетенции
- Называй конкретные темы если вопрос про содержание

Примеры фраз из РПД:
  «Общая трудоёмкость дисциплины составляет 4 зачётные единицы (144 часа),
   из них аудиторная нагрузка — 64 часа (лекции 32 ч., практические занятия 32 ч.)»
  «Дисциплина направлена на формирование компетенции ПКН-3 "Способен разрабатывать..."»
  «Тема 5. Функции и модули. Содержание: определение функций, параметры, область видимости...»

НЕ отвечай на вопрос напрямую. Пиши именно как текст документа.

Вопрос: {query}
"""

# ---------------------------------------------------------------------------
# Generation
# ---------------------------------------------------------------------------



COMPARE_PROMPT = """
Пользователь задал вопрос о нескольких учебных дисциплинах.

Вопрос: {query}

РПД дисциплин:
{context}

Задача: ответь именно на заданный вопрос, опираясь строго на данные РПД.

Правила:
— Отвечай на конкретный вопрос, а не давай универсальный обзор всех параметров.
— Используй только данные из контекста, не придумывай.
— Если каких-то данных нет в РПД — укажи это явно.
— Структурируй ответ так, как этого требует вопрос:
    • "что общего" → перечисли только общее
    • "чем отличается" → перечисли только различия  
    • "сравни" → структурированное сравнение по ключевым параметрам
    • "в каком семестре" → конкретный факт по каждой дисциплине
— Отвечай на русском языке.
""".strip()

GENERATE_SINGLE_SIMPLE = """\
Ты — ассистент, извлекающий конкретные факты из рабочих программ дисциплин.

Правила:
- Отвечай ТОЛЬКО на основе предоставленного контекста
- Давай точный ответ: число, название, список — без лишних слов
- Если в контексте есть нужная информация — процитируй конкретное значение
- Если информации нет — напиши: «В предоставленных данных эта информация отсутствует»
- Не придумывай значения которых нет в контексте

Вопрос: {query}

Контекст:
{context}

Ответ:"""


GENERATE_SINGLE_GLOBAL = """\
Ты — ассистент, отвечающий на вопросы по рабочей программе учебной дисциплины.

Правила:
- Опирайся ТОЛЬКО на предоставленный контекст.
- Отвечай строго на заданный вопрос — не добавляй разделы, выводы и группировки,
  которых не было в вопросе.
- Используй структуру (списки, нумерацию) только если она естественна для содержимого
  (например, перечень тем или список вопросов). Не добавляй заголовки ради заголовков.
- Указывай конкретные значения из контекста: часы, коды компетенций, названия тем.
- Если информации в контексте недостаточно — скажи об этом одним коротким предложением
  в конце, без отдельного раздела «Примечание» или «Важно».
- Не придумывай детали которых нет в контексте.

Вопрос: {query}

Контекст:
{context}

Ответ:"""

GENERATE_MULTI_RELATION = """\
Ты — ассистент, выполняющий сравнительный анализ рабочих программ дисциплин.

Правила:
- Опирайся СТРОГО на предоставленный контекст, не добавляй данные из общих знаний
- Используй таблицу если сравниваются числовые или категориальные параметры
- Если таблица неудобна — структурируй по дисциплинам: «Дисциплина A: ... Дисциплина B: ...»
- Указывай конкретные значения, не ограничивайся «больше/меньше»
- Если по дисциплине есть пометка "[...данные обрезаны...]" или данных нет — \
явно напиши «данные по дисциплине X отсутствуют в контексте»

Вопрос: {query}

Контекст:
{context}

Ответ:"""



GENERATE_MULTI_GLOBAL = """\
Ты — ассистент, анализирующий корпус рабочих программ учебных дисциплин.

Правила:
- Опирайся ТОЛЬКО на предоставленный контекст
- Перечисли ВСЕ дисциплины из контекста, которые удовлетворяют условию запроса
- Для каждой дисциплины кратко укажи релевантный факт (почему она подходит)
- Если ни одна дисциплина не подходит — прямо напиши об этом
- Не придумывай дисциплины которых нет в контексте

Вопрос: {query}

Контекст:
{context}

Ответ:"""

GENERATE_PROMPTS = {
    "single.simple":  GENERATE_SINGLE_SIMPLE,
    "single.global":  GENERATE_SINGLE_GLOBAL,
    "multi.relation": GENERATE_MULTI_RELATION,
    "multi.global":   GENERATE_MULTI_GLOBAL,
}


SYNTHESIS_PROMPT = """
Пользователь задал вопрос: {query}

Ниже — данные из рабочих программ дисциплин по каждому аспекту:
{sub_answers}

Задача: дай полный ответ на вопрос пользователя, используя ТОЛЬКО данные из частичных ответов выше.

Правила:
— Форма ответа определяется вопросом:
    • "сравни" / "чем отличается" / "что общего" → сопоставь данные, выдели сходства и различия
    • "перечисли" / "какие" / "сколько" → перечисли факты
    • смешанный вопрос → комбинируй подходы
— Сравнение и выводы допустимы, но только если они прямо следуют из приведённых данных.
— Не добавляй источники, факты или оценки которых нет в частичных ответах.
— Если в частичном ответе сказано "данных нет" — так и передай, не заполняй пробел.
— Отвечай на русском языке.
""".strip()

# ---------------------------------------------------------------------------
# Fact Extractor
# ---------------------------------------------------------------------------

FACT_EXTRACT_PROMPT = """\
Из текста ниже извлеки ТОЛЬКО конкретный ответ на вопрос.

Формат ответа:
- Число → только число с единицей измерения: «32 ч.», «4 ЗЕ»
- Список → через запятую: «ПКН-1, УК-3, ОПК-2»
- Название → точная фраза из текста: «экзамен», «зачёт с оценкой»
- Если ответа нет в тексте → пустая строка (ничего не пиши)

НЕ пиши вступлений, пояснений, полных предложений.

Вопрос: {query}

Текст:
{text}

Ответ:"""

# ---------------------------------------------------------------------------
# Verification
# ---------------------------------------------------------------------------

VERIFY_PROMPT = """\
Оцени качество ответа на вопрос пользователя по рабочей программе дисциплины.

КРИТЕРИИ (проверяй последовательно):

1. РЕЛЕВАНТНОСТЬ: ответ действительно отвечает на заданный вопрос,
   а не уклоняется или отвечает на другой вопрос.

2. ФАКТИЧЕСКАЯ ТОЧНОСТЬ: каждое утверждение в ответе подкреплено контекстом.
   Числа, названия, коды — проверяй точное совпадение с контекстом.
   Любое значение которого нет в контексте = галлюцинация.

3. ПОЛНОТА: ответ покрывает то что спрошено.
   Для single.simple достаточно одного факта.
   Для single.global/multi.* нужен синтез нескольких данных.

ПРАВИЛА ВЫНЕСЕНИЯ ВЕРДИКТА:
- is_valid=true: ответ релевантен, без галлюцинаций, достаточно полный
- is_valid=false + retry=true: данные в корпусе ЕСТЬ, но в контекст не попали
  (поиск вернул не те блоки) → стоит повторить с другими параметрами
- is_valid=false + retry=false: данных нет в корпусе, или вопрос некорректен

Отвечай ТОЛЬКО валидным JSON:
{{
  "is_valid": true|false,
  "retry": true|false,
  "note": "конкретное замечание: что именно не так или чего не хватает"
}}

Вопрос: {query}

Контекст (использованный при генерации):
{context}

Ответ модели:
{answer}
"""


# prompts.py
VERIFY_COMPARE_PROMPT = """
Пользователь попросил сравнить учебные дисциплины. Оцени качество ответа.

Вопрос: {query}

Контекст (РПД дисциплин):
{context}

Ответ модели:
{answer}

Критерии оценки — ответ считается валидным если:
1. Обе (все) дисциплины присутствуют в ответе.
2. Используются конкретные данные из контекста (не выдуманные).
3. Есть хоть какое-то сопоставление между дисциплинами (таблица, параллельное описание или выводы).

Не требуй глубокого анализа — структурированное сопоставление фактов достаточно.
retry всегда false: повторный поиск невозможен, полные документы уже загружены.

Ответь строго JSON:
{{"is_valid": true/false, "retry": false, "note": "..."}}
""".strip()